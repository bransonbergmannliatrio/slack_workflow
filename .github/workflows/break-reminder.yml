name: Break reminder

on:
  schedule:
    - cron: '30 21 * * 1-5'  # 2:30 PM PDT, Mon–Fri

jobs:
  notify:
    uses: ./.github/workflows/slack-dm.yml
    with:
      message: "Time for a 10-minute break!"
    secrets: inherit
    